# CHECKLIST

완료 판정 기준. **전부 통과해야 커밋한다.** 하나라도 못 지켰으면 못 지켰다고 쓴다.

---

## CLONE 완료 판정

### 파일
- [ ] `refs/`에 스크린샷 5장 이상 (전체 1 + 섹션 4 이상)
- [ ] `SPEC.md` 8개 항목(그리드/타이포/아이콘/컬러/간격/모션/말투/섹션순서) 전부 채움
- [ ] `index.html` 단일 파일로 열림
- [ ] `REVIEW.md` 장점/단점/수정안 전부 채움

### 시각 대조 — `refs/`와 나란히 놓고 확인
- [ ] 섹션 순서와 개수가 원본과 같다
- [ ] 히어로 영역의 글자 크기 비율이 원본과 같다 (절대값 아니라 비율)
- [ ] 컬러가 원본과 같다 (스포이드로 찍어 확인)
- [ ] 섹션 상하 여백이 원본과 같은 리듬이다
- [ ] 폰트가 원본이거나, 같은 인상의 대체폰트다 (대체했으면 SPEC에 기록)
- [ ] 모션이 있다면 방향·속도·시차가 원본과 같다

### 규칙 준수
- [ ] 원본 텍스트를 그대로 쓴 곳이 없다
- [ ] 원본 이미지/로고를 가져온 게 없다
- [ ] `Lorem ipsum`이 없다
- [ ] PROTOCOL 6절의 금지 문구가 없다
- [ ] 더미 텍스트 길이가 원본과 같은 대역이다
- [ ] 더미 말투가 업종에 맞고 명사종결이 절반 이상이다 (PROTOCOL 5절)

### 동작
- [ ] 콘솔 에러 없음
- [ ] 1440px에서 의도대로 보인다
- [ ] 390px(모바일)에서 깨지지 않는다 — 단, 원본이 대응 안 하면 안 해도 된다

---

## BUILD 완료 판정

### 파일
- [ ] `DESIGN.html` 기획서가 있고, **코딩 전에** 사람 확인을 받았다
- [ ] `index.html` 단일 파일로 열림
- [ ] `NOTES.md` 라이브러리/막힌점/재사용여부 전부 채움

### 내용
- [ ] 그 주 CONCEPT 중 하나에서 출발했고, 어느 것인지 NOTES에 적혀 있다
- [ ] 그 주 리서치 결과를 **실제로** 적용했고, 무엇을 적용했는지 NOTES에 적혀 있다
      (라이브러리든 CSS 기법이든 상관없다. import만 해놓고 안 쓴 건 안 쓴 것이다)
- [ ] 실무에 그대로 낼 수 있는 완성도다. 미완성 섹션이 없다
- [ ] `index.html`이 `DESIGN.html`의 토큰만 쓴다. 즉흥으로 추가한 색·크기가 없다
- [ ] 참고한 디자인 시스템이 링크와 함께 기획서에 적혀 있다
- [ ] 기획서와 어긋난 지점이 `NOTES.md`에 적혀 있다

### 규칙 준수
- [ ] PROTOCOL 6절의 금지 문구가 없다
- [ ] 더미 텍스트가 구체적이다 (추상적 마케팅 문구 아님)

### 동작
- [ ] 콘솔 에러 없음
- [ ] 1440px / 768px / 390px 세 폭에서 다 정상
- [ ] `prefers-reduced-motion` 대응 (모션이 있다면)
- [ ] 본문 대비비 4.5:1 이상

---

## RESEARCH 완료 판정

- [ ] `raw.md`를 실제로 읽었다 (제목만 훑지 않았다)
- [ ] 어워드 사이트를 헤드리스 Chrome으로 직접 캡처해서 봤다
- [ ] `concepts.md`에 컨셉 7개, 서로 방향이 겹치지 않는다
- [ ] 각 컨셉에 참고 링크와 쓸 라이브러리가 붙어 있다
- [ ] 7개 중 최소 3개는 `raw.md`에서 새로 발견한 것에서 나왔다
      (기존에 알던 것만으로 채우면 리서치를 한 게 아니다)

### 노트와 출처
- [ ] `notes/` 4종(voice·icons·type·motion)을 썼다. 변동 없으면 근거와 함께 그렇게 적었다
- [ ] `sources.md`에 노트별 버전·조사일·재조사 시점이 표로 있다
- [ ] URL을 적을 땐 **실제로 요청해서 응답 코드를 확인**하고 적었다
- [ ] 조사 방법과 한계(표본 수, 안 본 범위)를 적었다
- [ ] 수집 불가로 확정된 것은 이유와 함께 남겼다 (같은 조사를 반복하지 않기 위해)
