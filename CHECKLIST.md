# CHECKLIST

완료 판정 기준. **전부 통과해야 커밋한다.** 하나라도 못 지켰으면 못 지켰다고 쓴다.

---

## 모작 완료 판정

### 대상 자격 — 여기서 걸리면 나머지는 의미 없다
- [ ] 국제 공인 어워드 **수상작**이다 (Awwwards / FWA / CSSDA / Webby / Communication Arts)
- [ ] 어워드 출처 URL과 수상 등급이 `SPEC.md` 맨 위에 적혀 있다
- [ ] "잘 만든 것 같아서" 고른 게 아니다

### 파일
- [ ] `refs/`에 스크린샷 5장 이상 (전체 1 + 섹션 4 이상)
- [ ] 멀티페이지면 **페이지마다** refs가 따로 있다
- [ ] **기능의 상태별 refs가 있다** (드롭다운 닫힘/열림, 탭 1/2, 모달 닫힘/열림)
- [ ] `SPEC.md` 항목 전부 채움 — 어워드출처 / 사이트구조 / 그리드 / 타이포 / 아이콘 /
      컬러 / 간격 / 모션 / **기능 목록** / 섹션순서
- [ ] `index.html`이 단일 파일로 열림. 멀티페이지면 하위 페이지도 전부 열림
- [ ] `REVIEW.md` 장점 / 단점 / 수정안 / **왜 상을 받았나** 전부 채움

### 시각 대조 — `refs/`와 나란히 놓고 확인
- [ ] 섹션 순서와 개수가 원본과 같다
- [ ] 히어로 영역의 글자 크기 비율이 원본과 같다 (절대값 아니라 비율)
- [ ] 컬러가 원본과 같다 (스포이드로 찍어 확인)
- [ ] 섹션 상하 여백이 원본과 같은 리듬이다
- [ ] 폰트가 원본이거나, 같은 인상의 대체폰트다 (대체했으면 SPEC에 기록)
- [ ] 모션이 있다면 방향·속도·시차가 원본과 같다

### 기능 — Playwright MCP로 실제로 눌러서 확인한다

> 코드를 보고 판단하지 않는다. 눌러본 결과로만 체크한다.

- [ ] 드롭다운 / 메가메뉴가 **열리고 닫힌다** (바깥 클릭·ESC 포함)
- [ ] 모바일 버거 메뉴가 열리고 닫히며, 열린 동안 배경 스크롤이 막힌다
- [ ] 탭이 **내용을 바꾼다**. 활성 표시가 따라간다
- [ ] 아코디언 / FAQ가 펼쳐지고 접힌다
- [ ] 캐러셀이 좌우로 움직이고 인디케이터가 따라간다
- [ ] 모달이 열리고, 바깥 클릭·ESC·닫기 버튼으로 닫힌다
- [ ] 필터 / 정렬이 실제로 목록을 바꾼다
- [ ] 폼에 입력·포커스가 되고, 제출은 막되 검증 메시지는 보인다
- [ ] **원본에 있는 기능 중 빠뜨린 게 없다** (SPEC 기능 목록과 1:1 대조)

### 내비게이션
- [ ] `href="#"`로 막아둔 링크가 없다
- [ ] 만든 페이지끼리 실제로 오갈 수 있다
- [ ] 현재 페이지 표시(active)가 맞게 뜬다

### 접근성 최소선
- [ ] 키보드만으로 메뉴·탭·모달을 열고 닫을 수 있다 (`Tab` / `Enter` / `ESC`)
- [ ] `aria-expanded` · `aria-selected` 가 실제 상태를 따라간다
- [ ] 포커스 링이 보인다
- [ ] 본문 대비비 4.5:1 이상
- [ ] `prefers-reduced-motion` 대응 (모션이 있다면)

### 규칙 준수
- [ ] 원본 텍스트를 그대로 쓴 곳이 없다
- [ ] 원본 이미지 / 로고를 가져온 게 없다
- [ ] **더미 언어가 원본과 같다** (영문 사이트면 영문)
- [ ] `Lorem ipsum`이 없다
- [ ] PROTOCOL 6절의 금지 문구가 없다
- [ ] 더미 텍스트 길이가 원본과 같은 대역이다

### 말투 (PROTOCOL 5절)
- [ ] 원본의 말투를 따랐다. 내 문체를 얹지 않았다
- [ ] 메뉴·섹션제목·카드제목·버튼이 **단어(명사구)로 끝난다**
- [ ] 꾸미는 말이 없다 — **단어를 하나씩 지워보고** 뜻이 안 변하는 건 뺐다
- [ ] 과장·최상급·마케팅 상투어가 없다
- [ ] 한 줄로 될 것을 두 줄로 쓰지 않았다

### 커밋 전 게이트
- [ ] `npx --yes impeccable@latest detect <파일>` 통과
      (걸린 규칙은 원본이 아니라 내가 흘린 AI 흔적이다)

### 동작
- [ ] 콘솔 에러 없음
- [ ] 가로 넘침 없음 — 1440 / 768 / 390
- [ ] 390px에서 깨지지 않는다 (원본이 대응 안 하면 안 해도 된다)

---

## 주간 구성 판정

- [ ] 주 10건 중 **멀티페이지 사이트가 4건 이상**이다
- [ ] 같은 스튜디오 / 같은 업종만 반복하지 않았다

---

## RESEARCH 완료 판정

- [ ] `raw.md`를 실제로 읽었다 (제목만 훑지 않았다)
- [ ] 어워드 수상작을 **실제로 긁어** `targets.md`를 만들었다
- [ ] 후보마다 실측했다 — 페이지 수 / 기능 유무 / WebGL / 문서 높이
- [ ] `targets.md`에 멀티페이지 후보가 4건 이상 들어 있다
- [ ] `concepts.md`에 컨셉 7개, 서로 방향이 겹치지 않는다
- [ ] 각 컨셉에 참고 링크가 붙어 있다

### 노트와 출처
- [ ] `notes/` 4종(icons·type·motion·voice)을 썼다. 변동 없으면 근거와 함께 그렇게 적었다
- [ ] `sources.md`에 노트별 버전·조사일·재조사 시점이 표로 있다
- [ ] URL을 적을 땐 **실제로 요청해서 응답 코드를 확인**하고 적었다
- [ ] 조사 방법과 한계(표본 수, 안 본 범위)를 적었다
- [ ] 수집 불가로 확정된 것은 이유와 함께 남겼다
