# PROTOCOL

이 문서가 **단일 원본**이다. 다른 어떤 파일과 충돌하면 이 문서가 이긴다.
AI는 작업 시작 전에 이 문서를 처음부터 끝까지 읽는다. 요약본으로 대체하지 않는다.

---

## 0. 이 시스템이 뭔가

웹사이트 **디자인** 훈련 아카이브다. 기술 스택 학습이 목적이 아니다.
"이 화면이 왜 좋아 보이는가"를 손으로 재현하면서 알아내는 게 목적이다.

산출물은 전부 **정적 HTML 한 장**이다. 빌드 단계 없음. 더블클릭하면 열려야 한다.

---

## 1. 작업 단위

**WORK** = 폴더 1개 = 슬롯 1칸.

| 종류 | 뭘 하나 |
|---|---|
| **CLONE** | 잘 만든 사이트를 더미 텍스트로 시각적 재현 → 해부 → 장단점 분석 |
| **BUILD** | 그 주 CONCEPT 중 하나를 골라, 라이브러리를 써서 실무용 페이지 제작 |

**주 5일(월~금), 하루 2슬롯 = 주 10 WORK.** 매일 CLONE 1 + BUILD 1.
RESEARCH(월요일 리서치)는 슬롯 밖이다. 10칸을 먹지 않는다.

---

## 2. 폴더 명명

```
works/<ISO주차>/<종류>-<번호>-<슬러그>/
```

- ISO주차: `2026-W38` 형식. ISO-8601 기준(월요일 시작).
- 종류: `clone` 또는 `build`
- 번호: 그 주 그 종류의 순번. `01`부터. 두 자리 고정.
- 슬러그: 소문자 영문/숫자/하이픈만. 원본 사이트명이나 컨셉명에서 딴다.

예: `works/2026-W38/clone-03-linear-app/`, `works/2026-W38/build-01-brutalist-editorial/`

---

## 3. CLONE 절차

순서를 건너뛰지 않는다. 특히 2번을 건너뛰고 바로 코딩하지 않는다.

### 3-1. 원본 확보
- 원본 URL을 `SPEC.md` 맨 위에 기록.
- 스크린샷을 `refs/`에 저장. **최소 5장**: 전체 페이지 1장 + 주요 섹션 4장 이상.
- 스크롤 인터랙션이 핵심인 사이트면 상태별로 더 뜬다(before/during/after).
- 파일명: `refs/00-full.png`, `refs/01-hero.png`, `refs/02-<섹션명>.png` ...

### 3-2. 해부 → `SPEC.md`
코딩 전에 쓴다. 눈으로 본 걸 수치로 옮기는 단계다.
템플릿: `templates/SPEC.md`

최소한 이걸 다 채운다.
- **그리드**: 컬럼 수, 거터, 최대폭, 좌우 여백, 브레이크포인트
- **타이포**: 폰트 패밀리(대체 포함), 크기 스케일 전부(px), 자간, 행간, 굵기
- **컬러**: 전부 hex로. 배경/전경/강조/보더 역할까지 적는다
- **간격**: 섹션 상하 패딩, 요소 간 마진. 반복되는 수치 = 그 사이트의 spacing scale
- **모션**: 무엇이 언제 어떻게 움직이나. duration, easing, trigger
- **섹션 순서**: 위에서 아래로 나열

### 3-3. 재현 → `index.html`
- 단일 HTML. CSS는 `<style>`에 인라인. JS 필요하면 `<script>`에 인라인.
- 외부는 **CDN만** 허용: `cdnjs.cloudflare.com`, `cdn.jsdelivr.net/npm/`, Google Fonts.
- 목표는 **시각적으로 똑같이**다. 코드 구조가 원본과 같을 필요 없다.
- 반응형은 원본이 하는 만큼만 한다. 원본에 없는 걸 추가하지 않는다.

### 3-4. 대조
- `refs/`의 스크린샷과 나란히 놓고 비교한다.
- 어긋난 곳을 찾아 고친다. **"대충 비슷하다"에서 멈추지 않는다.**
- 완료 판정은 `CHECKLIST.md`를 따른다.

### 3-5. 분석 → `REVIEW.md`
재현이 끝난 뒤에 쓴다. 템플릿: `templates/REVIEW.md`
- **장점**: 이 디자인이 실제로 잘 작동하는 지점. 왜 그런지 이유까지.
- **단점**: 안 좋은 지점. 취향 말고 근거를 댄다(가독성, 정보 위계, 접근성, 성능).
- **수정안**: 내가 고친다면 뭘 어떻게. 구체적인 수치로.

---

## 4. BUILD 절차

### 4-1. 컨셉 선택
- 그 주 `research/<주차>/concepts.md`의 7개 중 하나를 고른다.
- 이미 쓴 컨셉은 다시 쓰지 않는다.

### 4-2. 제작 → `index.html`
- 실무에서 쓸 수 있는 수준이어야 한다. 데모용 장난이 아니다.
- 그 주 리서치에서 나온 **라이브러리를 최소 1개 이상 실제로 쓴다.**
- CLONE과 같은 제약: 단일 HTML, 허용된 CDN만.

### 4-3. 기록 → `NOTES.md`
템플릿: `templates/NOTES.md`
- 쓴 라이브러리, 버전, 왜 골랐나
- 막힌 지점과 해결 방법
- 다음에 또 쓸 것인가, 아니라면 왜

---

## 5. 더미 데이터 규칙

CLONE은 원본의 **디자인**을 재현하는 것이지 **내용**을 베끼는 게 아니다.

- 원본 텍스트를 그대로 옮기지 않는다. 전부 더미로 바꾼다.
- 로고, 사진, 일러스트, 아이콘을 원본에서 가져오지 않는다.
  → 같은 비율의 플레이스홀더(단색 블록, CSS 도형, `picsum.photos`)로 대체.
- 더미 텍스트는 **원본과 같은 글자 수 대역**으로 맞춘다. 길이가 다르면 레이아웃이 깨진다.
- `Lorem ipsum`을 쓰지 않는다. 그 사이트의 업종에 맞는 한국어/영어 더미를 쓴다.
  (예: SaaS 랜딩이면 가상의 제품명과 그럴듯한 기능 설명)

---

## 6. 쓰지 말 것 (중요)

산출물에 아래가 들어가면 그 WORK는 실패로 친다.

- **공허한 마케팅 문구.** "혁신적인", "차별화된", "고객 중심의", "최적의 솔루션",
  "seamless", "cutting-edge", "empower", "unlock" 류. 더미 텍스트에도 쓰지 않는다.
  더미는 구체적이어야 한다. "빠른 배포" 대신 "커밋 후 40초 안에 배포".
- **이모지 남발.** 원본에 있으면 쓰고, 없으면 안 쓴다.
- **근거 없는 REVIEW.** "모던하고 세련됨" 같은 문장은 분석이 아니다.
  무엇이 어떤 수치로 어떻게 작동해서 그런 인상을 주는지 쓴다.
- **안 해놓고 했다고 하기.** 대조를 안 했으면 안 했다고 쓴다.

---

## 7. 커밋

- WORK 하나가 커밋 하나. 중간 저장이 필요하면 나눠도 되지만 마지막에 정리한다.
- 메시지 형식:
  ```
  clone(2026-W38): linear-app 재현
  build(2026-W38): brutalist-editorial
  research(2026-W38): 컨셉 7개
  ```
- 산출물 커밋 후 `node scripts/build-index.mjs`로 갤러리를 갱신한다.
  (푸시하면 Actions가 자동으로도 돈다)

---

## 8. RESEARCH (월요일)

**수집은 자동, 해석은 사람이 한다.** 이 경계를 넘지 않는다.

- 월요일 09:00 KST에 Actions가 `research/<주차>/raw.md`를 만들고 이슈를 연다.
- AI는 `raw.md`를 읽고 사람과 **대화하면서** 컨셉 7개를 뽑는다.
  혼자 7개를 만들어서 파일에 박아넣지 않는다. 그건 아무도 안 읽는 문서가 된다.
- 결과는 `research/<주차>/concepts.md`. 템플릿: `templates/CONCEPTS.md`
- 컨셉 7개는 서로 달라야 한다. 방향이 겹치면 6개로 줄이고 다시 뽑는다.
  참고 축: 3D/WebGL, 딥테크, 매거진/에디토리얼, 브루탈리즘, 미니멀,
  모션 중심, 데이터 시각화, 레트로, 타이포그래피 실험, 인터랙션 실험

---

## 9. 작업 시작 방법

사람이 AI에게 **"오늘 작업 시작"** 이라고 말한다. 그러면 AI는:

1. 이 문서를 읽는다
2. `works/<이번주차>/`를 확인해 오늘 몇 번째 슬롯인지 파악한다
3. 다음 WORK 종류(CLONE/BUILD)를 정하고 사람에게 확인받는다
4. 절차대로 진행한다
5. `CHECKLIST.md`로 완료를 판정하고 커밋한다

**3번의 확인을 건너뛰지 않는다.** 어떤 사이트를 모작할지, 어떤 컨셉을 빌드할지는
사람이 정한다.
