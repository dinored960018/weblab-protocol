# PROTOCOL

이 문서가 **단일 원본**이다. 다른 어떤 파일과 충돌하면 이 문서가 이긴다.
AI는 작업 시작 전에 이 문서를 처음부터 끝까지 읽는다. 요약본으로 대체하지 않는다.

> **2026-09-21 개정.** 창작(BUILD)을 폐기했다. 모작만 한다.
> 대상은 국제 공인 어워드 수상작으로 제한한다. 기능도 실제로 동작해야 한다.

---

## 0. 이 시스템이 뭔가

웹사이트 **디자인** 훈련 아카이브다.
**세계 최고 수준으로 인정받은 사이트를 손으로 재현하면서** 왜 그렇게 보이는지 알아내는 게 목적이다.

산출물은 **정적 HTML**이다. 빌드 단계 없음. 더블클릭하면 열려야 한다.

---

## 1. 작업 단위

**WORK** = 폴더 1개 = 슬롯 1칸 = **모작 1건**.

**주 5일(월~금), 하루 2슬롯 = 주 10 모작.**
RESEARCH(월요일 리서치)는 슬롯 밖이다. 10칸을 먹지 않는다.

창작은 하지 않는다. 디자인 시스템을 새로 만드는 작업은 이 아카이브의 일이 아니다.

---

## 2. 모작 대상 선정 — 여기가 제일 중요하다

### 허용되는 출처 (국제 공인 어워드만)

| 어워드 | 등급 | 목록 |
|---|---|---|
| **Awwwards** | SOTD / SOTM / SOTY / Honorable Mention | `awwwards.com/websites/sites_of_the_day/` |
| **FWA** | FWA of the Day / Month / Year | `thefwa.com/awards/fwa-of-the-day` |
| **CSS Design Awards** | WOTD / Best Innovation | `cssdesignawards.com/website-gallery` |
| **Webby Awards** | Winner / Honoree | `winners.webbyawards.com` |
| **Communication Arts** | Webpick of the Week | `commarts.com/webpicks` |

### 금지

- **국내 디자인 어워드로 대체하지 않는다.**
- **"잘 만든 것 같은" 상용 사이트를 임의로 고르지 않는다.** 수상 이력이 없으면 대상이 아니다.
- 수상 여부가 확인 안 되면 **고르지 않는다.** 추측으로 채우지 않는다.

### 기록 의무

`SPEC.md` 맨 위에 **어워드 출처와 수상 등급**을 URL과 함께 적는다.
없으면 그 WORK는 시작 자체가 잘못된 것이다.

```
- 원본 URL: https://cerebrium.ai/
- 어워드: Awwwards Site of the Day — https://www.awwwards.com/sites/cerebrium
- 수상일: 2026-09-xx
```

### 주간 구성 규칙

주 10건 중 **최소 4건은 멀티페이지 사이트**여야 한다.
원페이지 스크롤 사이트만 계속 하면 내비게이션·라우팅·하위 페이지 설계를 못 배운다.

---

## 3. 폴더 명명

```
works/<ISO주차>/clone-<번호>-<슬러그>/
```

- ISO주차: `2026-W38` 형식. ISO-8601 기준(월요일 시작).
- 번호: 그 주 순번. `01`부터. 두 자리 고정.
- 슬러그: 소문자 영문/숫자/하이픈만. 원본 사이트명에서 딴다.

멀티페이지 모작이면 하위 페이지를 폴더 안에 둔다.

```
works/2026-W38/clone-01-white-desert/
├─ index.html           홈
├─ experience.html      하위 페이지
├─ about.html
├─ refs/                원본 스크린샷 (페이지별로)
├─ SPEC.md
└─ REVIEW.md
```

---

## 4. 모작 절차

순서를 건너뛰지 않는다. 특히 2번을 건너뛰고 바로 코딩하지 않는다.

### 4-1. 원본 확보

- 어워드 출처와 원본 URL을 `SPEC.md` 맨 위에 기록.
- `scripts/capture.mjs`로 캡처한다. 섹션마다 스크롤 후 대기하므로 스크롤 등장형도 잡힌다.
- 스크린샷 **최소 5장**: 전체 1 + 주요 섹션 4 이상.
- **멀티페이지면 페이지마다 따로 캡처한다.** `refs/home/`, `refs/about/` 식으로 나눈다.
- **기능의 상태별로도 뜬다.** 드롭다운 닫힘/열림, 탭 1번/2번, 모달 닫힘/열림.
  이걸 안 뜨면 5번 단계에서 뭘 만들어야 하는지 모른다.

### 4-2. 해부 → `SPEC.md`

코딩 전에 쓴다. 눈으로 본 걸 수치로 옮기는 단계다. 템플릿: `templates/SPEC.md`

- **어워드 출처**: 어디서 무슨 상을 받았나. URL 포함
- **사이트 구조**: 페이지 몇 개인가. 내비게이션 계층. 무엇이 무엇으로 연결되는가
- **그리드**: 컬럼 수, 거터, 최대폭, 좌우 여백, 브레이크포인트
- **타이포**: 폰트 패밀리(대체 포함), 크기 스케일 전부(px), 자간, 행간, 굵기, 타입 배율
- **아이콘**: 어느 세트인가, 크기, 스트로크 굵기, 모서리 처리
- **컬러**: 전부 hex로. 배경/전경/강조/보더 역할까지
- **간격**: 섹션 상하 패딩, 요소 간 마진. 반복되는 수치 = 그 사이트의 spacing scale
- **모션**: 무엇이 언제 어떻게 움직이나. duration, easing, trigger, 스태거
- **기능 목록**: 드롭다운·탭·아코디언·캐러셀·모달·필터·폼. 각각 어떻게 열리고 닫히는가
- **섹션 순서**: 위에서 아래로 나열

### 4-3. 재현 → `index.html` (+ 하위 페이지)

- CSS는 `<style>`에, JS는 `<script>`에 인라인. 외부는 CDN으로 불러온다
  (cdnjs, jsdelivr, unpkg, Google Fonts 등).
- **기술 스택은 자유다.** 필요하면 뭐든 쓴다.
- 목표는 **시각적으로 똑같이**다. 코드 구조가 원본과 같을 필요 없다.
- 반응형은 원본이 하는 만큼만 한다. 원본에 없는 걸 추가하지 않는다.
- **멀티페이지는 실제로 링크가 걸려야 한다.** `href="#"`로 막아두지 않는다.
  모든 내비게이션 항목이 어딘가로 가거나, 최소한 만든 페이지로 간다.

### 4-4. 기능 구현 — 생략 불가

**모작은 정지 화면을 베끼는 게 아니다.** 아래는 전부 실제로 동작해야 한다.

| 기능 | 동작 기준 |
|---|---|
| **드롭다운 / 메가메뉴** | 호버 또는 클릭으로 열리고, 바깥 클릭·ESC로 닫힌다 |
| **모바일 메뉴 (버거)** | 열리고 닫힌다. 열린 동안 배경 스크롤을 막는다 |
| **탭** | 클릭하면 내용이 바뀐다. 활성 표시가 따라간다 |
| **아코디언 / FAQ** | 펼치고 접힌다 |
| **캐러셀 / 슬라이더** | 좌우 이동이 된다. 인디케이터가 따라간다 |
| **모달 / 라이트박스** | 열리고, 바깥 클릭·ESC·닫기 버튼으로 닫힌다 |
| **필터 / 정렬** | 실제로 목록이 걸러지거나 정렬된다 |
| **폼** | 입력과 포커스가 되고, 제출은 막되 검증 메시지는 보인다 |

**접근성 최소선**: 키보드로 조작되고(`Tab`/`Enter`/`ESC`), `aria-expanded`·`aria-selected`가
상태를 따라가며, 포커스 링이 보인다.

원본에 없는 기능을 만들지 않는다. **원본에 있는 기능을 빠뜨리지 않는다.**

### 4-5. 대조

- `refs/`의 스크린샷과 나란히 놓고 비교한다. 페이지마다, 상태마다.
- 어긋난 곳을 찾아 고친다. **"대충 비슷하다"에서 멈추지 않는다.**
- 완료 판정은 `CHECKLIST.md`를 따른다.

### 4-6. 분석 → `REVIEW.md`

재현이 끝난 뒤에 쓴다. 템플릿: `templates/REVIEW.md`

- **장점**: 이 디자인이 실제로 잘 작동하는 지점. 왜 그런지 이유까지
- **단점**: 안 좋은 지점. 취향 말고 근거(가독성, 정보 위계, 접근성, 성능, 조작성)
- **수정안**: 내가 고친다면 뭘 어떻게. 구체적인 수치로
- **왜 상을 받았나**: 심사가 무엇을 봤을지. 이건 수상작 모작에서만 쓸 수 있는 질문이다

---

## 5. 더미 데이터 규칙

원본의 **디자인**을 재현하는 것이지 **내용**을 베끼는 게 아니다.

- 원본 텍스트를 그대로 옮기지 않는다. 전부 더미로 바꾼다.
- 로고, 사진, 일러스트, 아이콘을 원본에서 가져오지 않는다.
  → 같은 비율의 플레이스홀더(단색 블록, CSS 도형, `picsum.photos`)로 대체.
- **언어는 원본 그대로 간다.** 영문 사이트면 영문 더미를 쓴다.
  한국어로 바꾸면 글자폭·자간·줄바꿈이 달라져 원본과 다른 화면이 된다.
- 더미 텍스트는 **원본과 같은 글자 수 대역**으로 맞춘다.
- `Lorem ipsum`을 쓰지 않는다. 그 사이트의 업종에 맞는 그럴듯한 더미를 쓴다.

### 말투

> **[작성 대기]** 사용자가 지정할 예정이다.
> 지정 전까지 AI는 **말투를 임의로 정하지 않는다.** 더미 카피를 써야 하면
> 먼저 물어본다.
>
> 지정되면 이 자리에 규칙이 들어가고, `templates/`와 `CHECKLIST.md`도 같이 고친다.

---

## 6. 쓰지 말 것

산출물에 아래가 들어가면 그 WORK는 실패로 친다.

- **공허한 마케팅 문구.** "혁신적인", "차별화된", "최적의 솔루션",
  "seamless", "cutting-edge", "empower", "unlock", "elevate", "transform" 류.
  더미 텍스트에도 쓰지 않는다. 구체적으로 쓴다.
- **이모지 남발.** 원본에 있으면 쓰고, 없으면 안 쓴다.
- **근거 없는 REVIEW.** "모던하고 세련됨" 같은 문장은 분석이 아니다.
- **`href="#"` 로 막아둔 내비게이션.** 기능 미구현을 링크로 숨기지 않는다.
- **안 해놓고 했다고 하기.** 대조를 안 했으면 안 했다고 쓴다.

---

## 7. 커밋

- WORK 하나가 커밋 하나.
- 메시지 형식:
  ```
  clone(2026-W38): cerebrium 재현
  research(2026-W38): 컨셉 7개
  ```
- 산출물 커밋 후 `node scripts/build-index.mjs`로 내부 갤러리를,
  `node scripts/publish.mjs`로 공개 갤러리를 갱신한다.

---

## 8. RESEARCH (월요일)

**수집은 자동, 해석은 사람이 한다.** 이 경계를 넘지 않는다.

### 산출물

```
research/<주차>/
├─ raw.md        [자동] 원재료
├─ npm.json      [자동] 다음 주 증감 계산용
├─ sources.md    ★ 출처 원장. 무엇을 어디서 어떻게 확인했나
├─ targets.md    ★ 이번 주 모작 대상 10건. 어워드 출처·구조·기능 실측 포함
├─ concepts.md     컨셉 7개 (사람이 해석)
└─ notes/
    ├─ icons.md    아이콘 라이브러리 판세
    ├─ type.md     폰트 구조 (CDN 실제 응답 확인 포함)
    ├─ motion.md   CSS/HTML 모션 라이브러리
    └─ voice.md    카피 말투 (사용자 지정 규칙 기준)
```

### 규칙

- 월요일 09:00 KST에 Actions가 `raw.md`를 만들고 이슈를 연다.
- **어워드 수상작을 실제로 긁어 `targets.md`를 만든다.** 추측으로 고르지 않는다.
  각 후보에 대해 실측한다: 페이지 수, 드롭다운·탭·모달 유무, WebGL 여부, 문서 높이.
  그래야 "멀티페이지 최소 4건" 규칙을 지킬 수 있다.
- AI는 `raw.md`를 읽고 사람과 **대화하면서** 컨셉 7개를 뽑는다.
  혼자 7개를 만들어서 파일에 박아넣지 않는다.
  **컨셉은 만들 것이 아니라 볼 렌즈다.** 이번 주 수상작에서 무엇을 봐야 하는지의 목록이다.
- 컨셉 7개는 서로 달라야 한다. 방향이 겹치면 6개로 줄이고 다시 뽑는다.
  참고 축: 3D/WebGL, 딥테크, 매거진/에디토리얼, 브루탈리즘, 미니멀,
  모션 중심, 데이터 시각화, 레트로, 타이포그래피 실험, 인터랙션 실험
- **`notes/` 4종은 매주 쓴다.** 바뀐 게 없으면 "변동 없음"과 근거를 적는다.
- 어워드 사이트는 기계로 못 읽는다. 헤드리스 Chrome으로 직접 캡처해서 본다.

### 출처를 남기는 법 — `sources.md`

**추측을 사실처럼 적지 않는다.** 확인한 방법을 같이 남긴다.

- 노트별 **버전 · 조사일 · 재조사 시점**을 표로 관리한다
- URL과 **실제 응답 코드**를 적는다
- 수집 불가로 확정된 것은 이유와 함께 남긴다
- 조사 방법(도구, 옵션, 표본 수)과 **한계**를 적는다

---

## 9. 작업 시작 방법

사람이 AI에게 **"오늘 작업 시작"** 이라고 말한다. 그러면 AI는:

1. 이 문서를 읽는다
2. `works/<이번주차>/`를 확인해 오늘 몇 번째 슬롯인지 파악한다
3. `research/<주차>/targets.md`에서 다음 대상을 제안하고 **사람에게 확인받는다**
4. 절차대로 진행한다 (캡처 → 해부 → 재현 → 기능 → 대조 → 분석)
5. `CHECKLIST.md`로 완료를 판정하고 커밋한다

**3번의 확인을 건너뛰지 않는다.** 어떤 사이트를 모작할지는 사람이 정한다.
